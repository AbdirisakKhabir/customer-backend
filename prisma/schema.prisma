generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Customer {
  id        Int      @id @default(autoincrement())
  name      String
  phone     String   @unique // Phone number should be unique
  address   String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
